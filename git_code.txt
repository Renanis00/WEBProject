>git clone https://github.com/Renanis00/WEBProject (única vez)

>git config --global user.email "email@example.com" (configurar email do git única vez)

>git config --global user.name "John Doe"(configurar nome de usuário do git única vez)

>git remote https://github.com/Renanis00/WEBProject.git master (configurar repositório única vez)

>git init (para iniciar o repositório)

>git pull (atualizar e puxar)

~~~~~~~~~~~~~~~~ FAZENDO AS ALTERAÇÕES ~~~~~~~~~~~~~~~~~ 

>git status (para ver quais os arquivos devem ser adicionados)

>git add <arquivo> (adicionar arquivos para commit)

>git commit -m "exemplo comentários" (commitar e deixar comentário sobre)

>git push (enviar para o diretório)

>exit 

